# TraeCode CN 增强助手 使用说明

版本：1.0.0

TraeCode CN 增强助手是一个 Windows 本地工具，用来管理多个 Trae CN（TraeCode CN）IDE 账号、一键切换登录、自动签到、自动维护登录信息，并把账号状态展示在 Trae CN 窗口内的一个浮动面板里。

所有账号数据只保存在你这台电脑上，不会上传到任何服务器。

---

## 一、它能做什么

- **多账号管理**：在本地保存多个 Trae CN 账号，一键切换，不用反复扫码登录。
- **无感登录**：添加新账号时不用退出当前账号，在浏览器完成授权即可。
- **假退出登录**：传统方式登录新账号，先备份当前账号再进入 Trae CN 登录页。
- **自动签到**：每天自动领取签到积分，支持 15 / 30 / 60 / 120 分钟检查一次。
- **登录信息保活**：只在登录信息临近过期时才更新，平时复用现有凭据，避免把其他设备顶下线。
- **账号迁移**：用密码加密导出账号，在另一台电脑上导入。
- **额度查看**：在面板里直接看每个账号的套餐、剩余额度、签到状态、登录有效期。
- **关闭 Trae CN 自动更新**：避免更新过程打断你正在进行的会话。

---

## 二、系统要求

- Windows 10 或 Windows 11（64 位）
- 已安装 Trae CN 主程序
- 不需要管理员权限

---

## 三、安装步骤

### 1. 运行安装包

双击 `TraeCodeEnhancer-Setup-1.0.0.exe`。

如果 Windows SmartScreen 弹出警告（安装包未签名），点击「更多信息」→「仍要运行」即可。

### 2. 选择安装位置

默认安装到 `%LOCALAPPDATA%\Programs\TraeCode CN Enhancer`，可以在安装向导的目录页改成其他位置。

### 3. 确认 Trae CN 主程序位置

安装向导会自动检测 Trae CN 主程序（`Trae CN.exe`）的位置。

- 如果检测到了，会显示路径，直接点「下一步」即可。
- 如果没检测到，点「浏览」手动选择 `Trae CN.exe` 文件。文件名必须是 `Trae CN.exe`，否则会被拒绝。

### 4. 选择附加任务

- **创建桌面快捷方式**：在桌面放一个启动图标。
- **登录时自动启动后台服务**：开机后自动运行后台服务，用于定时签到和保活。推荐勾选。

### 5. 安装完成

安装结束时会自动启动 Trae CN 和增强面板。Trae CN 窗口右下角会出现一个浮动按钮。

---

## 四、首次使用

### 1. 打开面板

Trae CN 启动后，窗口右下角有一个小机器人图标（浮动按钮）。点一下就打开增强助手面板。

### 2. 自动纳管当前账号

如果你在 Trae CN 里已经登录了一个账号，打开面板时助手会自动把这个账号备份到本地。备份完成后，面板的「账号」标签页里会出现这个账号的卡片。

如果自动纳管失败，面板会显示「自动纳管当前账号失败」并给出原因，点「重新尝试」即可重试。

### 3. 检查面板状态

面板底部的状态栏会显示连接情况：

- **CDP 已连接**：面板与 Trae CN 已建立连接，一切正常。
- **等待 CDP**：Trae CN 还没启动或连接还没建立，稍等一下。
- **服务不可用**：后台服务没起来，看后面的「故障排查」。

---

## 五、功能详解

面板顶部有三个标签页：**账号**、**设置**、**关于**。

### 5.1 账号标签页

这是主页面，用来管理你的账号。

#### 工具栏（顶部一行按钮）

- **账号数 / 总积分**：显示已保存账号数量和所有账号剩余积分总和。
- **刷新**：重新拉取所有账号的最新状态。
- **登录**：添加新账号（见下文「添加账号」）。
- **导出**：把账号加密导出到一个文件（见下文「导出账号」）。
- **导入**：从导出文件导入账号（见下文「导入账号」）。
- **立即签到**：给今天还没签到的账号补领一次签到奖励，不会切换当前账号。

#### 账号卡片

每个账号显示为一张卡片：

- **名称**：账号显示名。
- **当前**：如果这个账号正登录在 Trae CN 里，显示「当前」标记。
- **切换按钮**（双向箭头图标）：点击切换到此账号。为了防止误操作，第一次点击只是把按钮变深，3 秒内再点一次才真正切换。
- **删除按钮**（垃圾桶图标）：删除这个账号的本地备份。当前正在使用的账号受保护，不能删除。
- **手机**：账号绑定的手机号。
- **套餐**：当前套餐类型。
- **签到**：今日签到状态。可能是「已签到」「待签到」「同步中」「失败」。
- **有效期至**：登录信息（access token）的过期时间。临近过期会标红。
- **剩余**：剩余额度。

#### 添加账号

点工具栏的「登录」按钮，会弹出选择登录方式对话框，有两种方式：

**方式一：无感登录（推荐）**

- 不退出 Trae CN，在浏览器完成授权后新账号自动加入列表。
- 点了之后会弹出登录进度对话框，并在浏览器打开授权页面。
- 在浏览器里用新账号完成授权登录。
- 登录成功后，新账号立即出现在列表，并自动同步套餐、余额、签到状态、登录有效期。

**方式二：假退出**

- 先安全备份当前账号，再让 Trae CN 重启到登录页。
- 在 Trae CN 登录页用新账号登录。
- 登录成功后新账号自动加入列表。

#### 切换账号

- 在目标账号卡片右侧点切换按钮。
- 第一次点击按钮变深色，3 秒内再点一次才真正切换。
- 切换过程是事务式的：先备份当前账号，再替换登录状态，失败会自动回滚到原账号。
- 切换时只替换账号相关的登录状态，你的工作区、窗口状态、扩展和 Trae CN 其他设置都不会动。

#### 删除账号

- 点账号卡片上的垃圾桶图标。
- 弹出确认对话框，输入「删除」两个字才能点「删除」按钮。
- 只删除本地备份，不会影响 Trae CN 当前登录。
- 当前正在使用的账号不能删除。

#### 导出账号

- 点工具栏的「导出」按钮。
- 输入一个密码，导出文件会用这个密码加密（scrypt + AES-256-GCM）。
- 请把密码单独保存好，丢了密码就没办法导入。
- 导出是账号迁移，不是多设备共享。原设备继续刷新登录信息后，另一台设备上的副本可能失效，需要重新登录。

#### 导入账号

- 点工具栏的「导入」按钮。
- 选择之前导出的 `.json` 文件，输入导出时设置的密码。
- 导入不会自动切换当前账号，你需要在列表里手动切换。

---

### 5.2 设置标签页

设置按「签到 / 更新 / 维护」三个分组组织，避免所有配置堆在一页。

#### 签到分组

- **自动签到**：开启或关闭。
- **检查间隔**：15 / 30 / 60 / 120 分钟。默认 30 分钟。
- **页面加载时补签**：开启后，打开面板时会立即检查并补签今天还没签到的账号。
- **保存**：改完点「保存」才生效。保存不会立即触发一次签到，只是更新定时计划。

说明：

- 每个账号每天最多领一次签到奖励，今天已经签到的会跳过。
- 每个账号用自己独立的 `userId` 作为设备标识，不会串号。
- 关闭自动签到只影响自动检查；点工具栏的「立即签到」属于手动操作，不受这个开关限制。

#### 更新分组

- **Trae CN 更新模式**：
  - **禁止自动更新（推荐）**：把 Trae CN 的 `update.mode` 改成 `manual`，避免自动更新打断当前会话。仍然可以在 Trae CN 菜单里手动检查更新。
  - **允许自动更新**：恢复 Trae CN 的原更新设置（会把之前保存的原始模式还原回去）。
- **保存**：改完点「保存」。
- 重启 Trae CN 后才生效，保存后会显示「已保存，重启 Trae CN 后生效」提示。

这个设置只定点写入 Trae CN 的 `User\settings.json` 里的 `update.mode` 一项，会保留注释和其他设置。第一次写入前会自动备份原文件到 `settings.json.trae-enhancer.bak`，写失败会自动还原。

#### 维护分组

- **重启后台服务**：如果面板不响应或后台服务卡住，点这里单独重启助手自己的后台服务。
- 重启不会退出 Trae CN，也不会修改当前登录账号。

---

### 5.3 关于标签页

面向普通用户的使用说明，包含：

- **添加账号**：怎么用无感登录加账号。
- **切换账号**：怎么点切换按钮。
- **迁移账号**：导出导入流程和注意事项。
- **自动签到**：每天自动核对签到状态。
- **登录信息维护**：只在临近过期时才更新。
- **数据安全**：导出文件是搬迁副本不是共享账号。

底部显示当前版本号。

---

## 六、系统托盘

安装后会在 Windows 任务栏右下角（系统托盘）显示一个图标。右键点击可以看到菜单：

- **启动 Trae CN 与面板**：启动 Trae CN 和增强面板。
- **启动后台服务**：只启动后台服务（不启动 Trae CN）。
- **重新启动后台服务**：重启后台服务。
- **停止后台服务**：停止后台服务（不会退出 Trae CN）。
- **查看状态**：打开控制台显示服务、CDP、端口等状态。
- **查看日志**：打开后台服务日志。
- **退出托盘**：退出托盘程序（不影响后台服务运行）。

---

## 七、命令行（进阶）

如果你喜欢命令行，安装目录下的 `scripts\trae-enhancer.cmd` 支持以下子命令。也可以用开始菜单里的「停止后台服务」等快捷方式。

| 命令 | 作用 |
| --- | --- |
| `start` | 启动 Trae CN、CDP、后台服务和面板注入。 |
| `daemon` | 只启动后台服务和守护进程。 |
| `stop` | 根据精确 PID 停止守护进程和后台服务。不会批量结束 Node、Electron 或 Trae CN 进程。 |
| `restart` | 先 `stop` 再 `daemon`。 |
| `status` | 显示服务、CDP、后台监督、自启动和端口状态。 |
| `locate` | 显示 Trae CN 路径和探测过程。 |
| `configure --trae-exe <路径>` | 保存或清除 Trae CN 可执行文件路径。 |
| `net` | 直接探测项目所需域名（DNS + HTTPS），不修改系统代理。 |
| `install` | 注册登录时自动启动后台服务。 |
| `uninstall` | 移除登录时自启动。 |
| `tray` | 启动托盘入口。 |
| `tray-stop` | 停止托盘入口。 |
| `logs` | 查看守护进程和后台监督日志。 |

示例：

```powershell
TraeCodeEnhancer.exe status
TraeCodeEnhancer.exe stop
TraeCodeEnhancer.exe configure --trae-exe "D:\Programs\Trae CN\Trae CN.exe"
```

---

## 八、升级和卸载

### 升级

直接运行新版安装包覆盖安装即可。账号数据和设置会保留。

升级时安装程序会先自动停止旧版后台服务，再替换文件。

### 卸载

在「设置 → 应用」里找到「TraeCode CN Enhancer」卸载，或运行安装目录下的卸载程序。

卸载时会：

1. 自动停止托盘和后台服务。
2. 移除登录时自启动。
3. 询问是否保留用户数据。

**关于用户数据**：`data\` 目录里有账号备份（含登录凭据）、配置和 API token。选择保留便于以后重装或换机后继续使用；选择删除会连同登录凭据一起彻底删除，无法恢复。

---

## 九、数据与安全

- 本地服务只监听 `127.0.0.1`，外部网络无法访问。
- 日志会过滤 token、JWT、授权头和敏感查询参数，不会记录访问令牌、刷新令牌、Cookie、私钥或完整认证快照。
- 导出文件使用 scrypt 派生密钥 + AES-256-GCM 加密。
- 删除账号只删除本地备份，不会退出 Trae CN 当前登录。
- 不修改 Trae CN 安装目录，不修改 `app.asar`。
- 切换账号时只替换账号相关的 `iCube*` 登录状态，工作区、窗口状态、扩展和其他 Trae CN 设置保持不变。

---

## 十、故障排查

### 面板没出现 / 浮动按钮找不到

1. 确认 Trae CN 已经启动（不是 TraeWork 或其他 TRAE 产品，它们是不同产品）。
2. 在托盘菜单点「查看状态」，看 `cdpConnected` 是不是 `true`。
3. 如果 CDP 没连接，重启后台服务：托盘菜单「重新启动后台服务」，或命令行 `TraeCodeEnhancer.exe restart`。
4. 还不行就完全重启：`TraeCodeEnhancer.exe stop` 然后重新启动。

### 面板显示「服务不可用」或「Failed to fetch」

后台服务没起来或被防火墙拦截。

1. 命令行运行 `TraeCodeEnhancer.exe status` 看服务状态。
2. 如果服务没运行，`TraeCodeEnhancer.exe daemon` 启动。
3. 检查本地端口 `47836` 是否被占用。

### 网络异常 / 签到失败

运行直连探测：

```powershell
TraeCodeEnhancer.exe net
```

这个命令只做 DNS 和 HTTPS 直连探测，不修改系统代理。失败时会显示完整的错误原因链，比如 `ECONNREFUSED`、`ENOTFOUND` 或证书错误。

### 查看日志

```powershell
TraeCodeEnhancer.exe logs
```

日志在安装目录的 `logs\` 下。日志里的敏感信息已经过滤过。

### 自动签到没生效

1. 进设置页确认「自动签到」是开启的。
2. 确认安装时勾选了「登录时自动启动后台服务」，或者手动运行过 `TraeCodeEnhancer.exe install`。
3. 检查后台服务是否在运行（`status` 命令）。

### 切换账号失败

切换是事务式的，失败会自动回滚。如果反复失败：

1. 看 `logs\daemon.log` 里切换过程的错误信息。
2. 确认目标账号的本地备份还在。
3. 如果当前 Trae CN 里的账号状态有问题，可以重新登录一次再备份。

---

## 十一、关于代理

本发布版本**不包含代理功能**，所有网络请求直连。如果你的网络环境必须通过代理访问 Trae CN 服务，这个版本无法使用。

---

## 十二、默认端口

- 本地服务地址：`http://127.0.0.1:47836`
- CDP 调试地址：`127.0.0.1:9336`

这两个端口只监听本机，外部无法访问。

---

如需进一步帮助，请把 `TraeCodeEnhancer.exe status` 和 `TraeCodeEnhancer.exe logs` 的输出发给提供给你这个工具的人。
